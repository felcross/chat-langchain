# Quick Fixes — FinanceBot BR (7 mudanças rápidas)

## Mudanças aprovadas

### 1. finance.db como volume Docker
- **Arquivo:** `docker-compose.yml`
- **Mudança:** Adicionar `- ./finance.db:/app/finance.db` na seção volumes

### 2. Corrigir hostname nginx
- **Arquivo:** `nginx.conf:15`
- **Mudança:** Trocar `http://financebot:8501` por `http://finance-bot:8501`

### 3. Cache path absoluto
- **Arquivo:** `cache_manager.py:28`
- **Mudança:** Trocar `CACHE_DIR = ".cache"` por `CACHE_DIR = str(Path(__file__).resolve().parent / ".cache")`
- Adicionar `from pathlib import Path` no import

### 4. SQL whitelist com regex
- **Arquivo:** `db_manager.py:144-150`
- **Mudança:** Substituir blacklist por regex whitelist: `re.match(r'^\s*(SELECT|WITH|SHOW|DESCRIBE|EXPLAIN)\b', sql, re.I)`
- Adicionar `import re`

### 5. Singleton LLM com @st.cache_resource
- **Arquivo:** `app.py:58-60`
- **Mudança:** Decorar `get_llm()` com `@st.cache_resource` para reutilizar instância

### 6. Criar .dockerignore
- **Arquivo:** `.dockerignore` (novo)
- **Conteúdo:** `.git`, `.venv`, `__pycache__`, `.cache/`, `query_logs.jsonl`, `.mimocode/`, `finance.db`, `.env`

### 7. Remover basicConfig duplicado
- **Arquivo:** `etl_bcb.py:27-31`
- **Mudança:** Remover bloco `logging.basicConfig(...)` — o app.py já configura

## Verificação
- Verificar que o docker-compose monta finance.db
- Verificar que nginx aponta para hostname correto
- Verificar que cache usa path absoluto
- Verificar que SQL whitelist funciona (apenas SELECT permitido)
- Verificar que get_llm() usa cache_resource
- Verificar que .dockerignore existe
- Verificar que etl_bcb.py não tem basicConfig duplicado
